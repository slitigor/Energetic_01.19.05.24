import { IConnection } from "@/data/types";

const ConnectionAddDialog = ({
  connectionList,
}: {
  connectionList: IConnection[];
}) => {
  return <div>ConnectionAddDialog</div>;
};

export default ConnectionAddDialog;
